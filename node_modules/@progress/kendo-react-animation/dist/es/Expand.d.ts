import * as React from 'react';
import * as PropTypes from 'prop-types';
import { AnimationInterface } from './AnimationInterface';
/**
 * Specifies the direction of the Expand Animation
 * ([see example]({% slug direction_animation %}#toc-expand)).
 *
 * The supported directions are:
 * * (Default) `vertical`&mdash;Expands the content from center to top and bottom, and vice-versa.
 * * `horizontal`&mdash;Expands the content from center to left and right, and vice-versa.
 */
export declare type ExpandDirection = 'horizontal' | 'vertical';
/**
 * Represent the props of the
 * [KendoReact Expand Animation component]({% slug animationtypes_animation %}#toc-expand).
 *
 * @example
 * ```html-preview
 * <style>
 * .content {
 *  width: 100px;
 *  padding: 10px;
 *  color: #787878;
 *  background-color: #fcf7f8;
 *  font-size: 13px;
 *  font-family: Helvetica, Arial, sans-serif;
 *  letter-spacing: 1px;
 *  text-align: center;
 *  border: 1px solid rgba(0,0,0,.05);
 * }
 * </style>
 * ```
 * ```jsx
 * class App extends React.Component {
 *     constructor(props) {
 *         super(props);
 *
 *         this.state = { show: false, direction: 'vertical' };
 *     }
 *
 *     onClick = () => {
 *         this.setState({
 *             show: !this.state.show
 *         });
 *     }
 *     onChange = (e) => {
 *         this.setState({ direction: e.target.value })
 *     }
 *
 *     render() {
 *         const { show, direction } = this.state;
 *
 *         const children = show ? (<div className="content">Content</div>) : null;
 *
 *         return (
 *             <div className="row">
 *                 <div className="col-md-6">
 *                     <dl>
 *                         <dt>
 *                             Expand: {direction}
 *                         </dt>
 *                         <dd>
 *                             <button className="k-button" onClick={this.onClick}>Animate</button>
 *                         </dd>
 *                     </dl>
 *                     <Expand direction={direction}>
 *                         {children}
 *                     </Expand>
 *                 </div>
 *                 <div className="col-md-6">
 *                     <dl>
 *                         <dt>
 *                             Direction:
 *                         </dt>
 *                         <dd>
 *                             <div className="example-config">
 *                                 <label>
 *                                     <input
 *                                          type="radio"
 *                                          name="direction"
 *                                          value="vertical"
 *                                          defaultChecked={this.state.direction === 'vertical'}
 *                                          onClick={this.onChange}
 *                                      />
 *                                     &nbsp;Vertical
 *                                 </label>
 *                                 <br />
 *                                 <label>
 *                                     <input
 *                                          type="radio"
 *                                          name="direction"
 *                                          value="horizontal"
 *                                          defaultChecked={this.state.direction === 'horizontal'}
 *                                          onClick={this.onChange}
 *                                      />
 *                                     &nbsp;Horizontal
 *                                 </label>
 *                             </div>
 *                         </dd>
 *                     </dl>
 *                 </div>
 *             </div>
 *         );
 *     }
 * }
 *
 * ReactDOM.render(
 *     <App />,
 *     document.querySelector('my-app')
 * );
 * ```
 */
export interface ExpandProps extends AnimationInterface {
    /**
     * After the element reached its exit state, it is no longer available in the DOM.
     * If a DOM operation is required, access it trough the `childFactory` function.
     */
    childFactory?: any;
    /**
     * Specifies the CSS class names which are set to the Animation.
     */
    className?: string;
    /**
     * Specifies the direction of the Expand Animation. Defaults to `vertical`.
     */
    direction?: ExpandDirection;
    /**
     * Specifies the node type of the parent Animation. Defaults to `div`.
     */
    component?: string;
    /**
     * Specifies the id of the Animation.
     */
    id?: string;
    /**
     * Specifies the style of the parent Animation.
     */
    style?: any;
}
export default class Expand extends React.Component<ExpandProps, {}> {
    /**
     * @hidden
     */
    static propTypes: {
        children: PropTypes.Requireable<any>;
        childFactory: PropTypes.Requireable<any>;
        className: PropTypes.Requireable<any>;
        direction: PropTypes.Requireable<any>;
        component: PropTypes.Requireable<any>;
        id: PropTypes.Requireable<any>;
        style: PropTypes.Requireable<any>;
    };
    /**
     * @hidden
     */
    static defaultProps: {
        appear: boolean;
        enter: boolean;
        exit: boolean;
        transitionEnterDuration: number;
        transitionExitDuration: number;
        direction: string;
    };
    /**
     * @hidden
     */
    render(): JSX.Element;
}
