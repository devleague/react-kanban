import * as React from 'react';
import * as PropTypes from 'prop-types';
import { AnimationInterface } from './AnimationInterface';
/**
 * Specifies the direction of the Zoom Animation
 * ([see example]({% slug direction_animation %}#toc-zoom)).
 *
 * The supported directions are:
 * * `in`&mdash;Zooms the content from the inside to the outside.
 * * (Default) `out`&mdash;Zooms the content from the outside to the inside.
 */
export declare type ZoomDirection = 'in' | 'out';
/**
 * Represent the props of the [KendoReact Zoom Animation component]({% slug animationtypes_animation %}#toc-zoom).
 *
 * @example
 * ```html-preview
 * <style>
 * .content {
 *    width: 100px;
 *    padding: 10px;
 *    color: #787878;
 *    background-color: #fcf7f8;
 *    font-size: 13px;
 *    font-family: Helvetica, Arial, sans-serif;
 *    letter-spacing: 1px;
 *    text-align: center;
 *    border: 1px solid rgba(0,0,0,.05);
 * }
 * </style>
 * ```
 * ```jsx
 * class App extends React.Component {
 *     constructor(props) {
 *         super(props);
 *
 *         this.state = { direction: "out", index: 1 };
 *     }
 *
 *     onClick = () => {
 *         this.setState({
 *             index: this.state.index + 1
 *         });
 *     }
 *
 *     onChange = (e) => {
 *         this.setState({
 *             direction: e.target.value
 *         });
 *     }
 *
 *     render() {
 *         const { direction, index } = this.state;
 *
 *         return (
 *             <div className="row">
 *                 <div className="col-md-6">
 *                     <dl>
 *                         <dt>
 *                             Zoom: {direction}
 *                         </dt>
 *                         <dd>
 *                             <button className="k-button" onClick={this.onClick}>Animate</button>
 *                         </dd>
 *                     </dl>
 *
 *                     <Zoom direction={direction} stackChildren={true}>
 *                         <div className="content" key={index}>{index}</div>
 *                     </Zoom>
 *                 </div>
 *                 <div className="col-md-6">
 *                     <dl>
 *                         <dt>
 *                             Direction:
 *                         </dt>
 *                         <dd>
 *                             <div className="example-config">
 *                                 <label>
 *                                     <input type="radio" name="direction" value="in" defaultChecked={this.state.direction === 'in'} onClick={this.onChange}/>
 *                                     &nbsp;In
 *                                 </label>
 *                                 <br />
 *                                 <label>
 *                                     <input type="radio" name="direction" value="out" defaultChecked={this.state.direction === 'out'} onClick={this.onChange}/>
 *                                     &nbsp;Out
 *                                 </label>
 *                             </div>
 *                         </dd>
 *                     </dl>
 *                 </div>
 *             </div>
 *         );
 *     }
 * }
 *
 * ReactDOM.render(
 *     <App />,
 *     document.querySelector('my-app')
 * );
 * ```
 */
export interface ZoomProps extends AnimationInterface {
    /**
     * After the element reaches its exit state, it is no longer available in the DOM.
     * If a DOM operation is required, access it trough the `childFactory` function.
     */
    childFactory?: any;
    /**
     * Specifies the CSS class names which are set to the Animation.
     */
    className?: string;
    /**
     * Specifies the direction of the Zoom Animation. Defaults to `down`.
     */
    direction?: ZoomDirection;
    /**
     * Specifies the node type of the parent Asnimation. Defaults to `div`.
     */
    component?: string;
    /**
     * Specifies the id of the Animation.
     */
    id?: string;
    /**
     * Specifies the style of the parent Animation.
     */
    style?: any;
    /**
     * Specifies whether the child elements will stack on top of each other without interfering.
     */
    stackChildren?: boolean;
}
/**
 * @hidden
 */
export interface ZoomState {
    style?: any;
}
export default class Zoom extends React.Component<ZoomProps, ZoomState> {
    /**
     * @hidden
     */
    static propTypes: {
        children: PropTypes.Requireable<any>;
        childFactory: PropTypes.Requireable<any>;
        className: PropTypes.Requireable<any>;
        direction: PropTypes.Requireable<any>;
        component: PropTypes.Requireable<any>;
        id: PropTypes.Requireable<any>;
        style: PropTypes.Requireable<any>;
        stackChildren: PropTypes.Requireable<any>;
    };
    /**
     * @hidden
     */
    static defaultProps: {
        appear: boolean;
        enter: boolean;
        exit: boolean;
        transitionEnterDuration: number;
        transitionExitDuration: number;
        direction: string;
        stackChildren: boolean;
    };
    /**
     * @hidden
     */
    readonly state: ZoomState;
    /**
     * @hidden
     */
    componentWillReceiveProps(): void;
    /**
     * @hidden
     */
    componentDidMount(): void;
    /**
     * @hidden
     */
    render(): JSX.Element;
    private componentWillEnter;
    private componentDidEnter;
    private updateContainerDimensions;
}
