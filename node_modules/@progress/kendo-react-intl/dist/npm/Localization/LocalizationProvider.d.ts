import * as React from 'react';
import * as PropTypes from 'prop-types';
import { LocalizationProviderProps } from './LocalizationProviderProps';
import { LocalizationService } from './LocalizationService';
/**
 * A React component which provides a localization service.
 * Expects a language string as a property of the component.
 */
export declare class LocalizationProvider extends React.Component<LocalizationProviderProps, {}> {
    /**
     * @hidden
     */
    static propTypes: {
        language: PropTypes.Requireable<any>;
    };
    /**
     * @hidden
     */
    static childContextTypes: {
        kendoLocalizationService: PropTypes.Requireable<any>;
    };
    /**
     * Returns a localization service.
     * The method is suitable for overriding when you
     * implement custom localization behavior.
     */
    getLocalizationService(): LocalizationService;
    /**
     * @hidden
     */
    getChildContext(): {
        kendoLocalizationService: LocalizationService;
    };
    /**
     * @hidden
     */
    render(): React.ReactElement<any>;
}
